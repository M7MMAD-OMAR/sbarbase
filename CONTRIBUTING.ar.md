[English](CONTRIBUTING.md)

# المساهمة

صباربيز قيد التطوير. قيمته في سلامة التشغيل: مشاريع Supabase كثيرة على خادم واحد، تنسخها احتياطيًا وتستعيدها وترقّيها دون خوف. يكتمل التغيير حين يُختبر بالقدر الذي يتطلبه خطره، وتُسجَّل أدلته، وتقول الوثائق ما يفعله وما لا يفعله.

## سير العمل

يمر كل تغيير بالخطوات الخمس نفسها. التغييرات الصغيرة تعبرها بسرعة، وتغييرات بيئة التشغيل تأخذ وقتًا أطول عن قصد.

1. **اختر العمل من خارطة الطريق.** [docs/engineering/plans/2026-09-23-roadmap.md](docs/engineering/plans/2026-09-23-roadmap.md) ترتّب العمل بحسب ما يحتاجه المستخدم أولًا. إن لم يكن تغييرك فيها، افتح بلاغًا (issue) واذكر مشكلة المستخدم التي يحلها.
2. **غيّر واختبر بالوحدات.** اقصر التغيير على شأن واحد. شغّل مجموعات الاختبار التي لا تشغّل حاويات:

   ```bash
   bun install --frozen-lockfile
   bun test
   bun run typecheck:ui
   DOCKER_HOST=unix:///var/run/docker.sock /usr/bin/python3 -m unittest discover -s lab -p 'test_*.py'
   ```

   كل عيب يحصل على اختبار يفشل قبل الإصلاح. وكل رفض جديد يحصل على اختبار للرفض نفسه، وآخر للحالة التي يجب أن يظل يقبلها.
3. **أثبته حيًا إن مسّ بيئة التشغيل.** أي شيء تحت `lab/` يشغّل الحاويات أو يوقفها أو يضبطها، وأي شيء في `deploy/`، وأي تغيير في القبول أو التجهيز أو السياج أو الاستعادة، يحتاج تشغيلًا حيًا:
   - على جهازك، الفحوص الحية المحدودة الموصوفة في [lab/README.ar.md](lab/README.ar.md) (اقرأه أولًا: بعض الفحوص تستهلك بيئات اختبار محفوظة)؛
   - لمسارات التثبيت والترقية، التجربة الكاملة على خادم فارغ في آلة افتراضية محلية: `lab/vm-rehearsal.sh --image <Fedora 44 Cloud qcow2>`. تنسخ الإيداع الحالي إلى آلة افتراضية جديدة، وتشغّل فحص القبول بأمر واحد مع أول مشروع، ثم تعيد التشغيل وتشترط عودة الخدمة.

   يكتب التشغيل الحي ملف JSON تحت `docs/evidence/`. أودعه: الأدلة هي طريقة القارئ اللاحق لمعرفة ما كان صحيحًا ومتى.
4. **راجع بعين المهاجم.** تغييرات بيئة التشغيل تحصل على مراجع ثانٍ، شخصًا كان أو وكيلًا، مهمته كسر التغيير: أوقفه بانهيار في منتصفه، شغّله مرتين، شغّله بالمستخدم الخطأ، على قسم بدل قرص، والمنفذ محجوز. سجّل ما وجده وما أُصلح. بعض العيوب في هذا المستودع لم تُكتشف إلا بهذه الطريقة، أو بتشغيل الأمر الموثّق على جهاز فارغ.
5. **حدّث الوثائق في التغيير نفسه.** الصفحة التي تشرح المجال (تجدها في [docs/README.ar.md](docs/README.ar.md))، والأرقام في [docs/reference/status.ar.md](docs/reference/status.ar.md)، وصف الجاهزية في [docs/reference/deployment-readiness.ar.md](docs/reference/deployment-readiness.ar.md) حين يتغير ادعاء. حدّث الصفحة العربية في التغيير نفسه، ويتحقق `lab/test_docs_bilingual.py` من الأزواج. يفشل `lab/test_docs_links.py` عند رابط مكسور أو شرطة طويلة، ويفشل `lab/test_doc_references.py` حين يذكر دليل تشغيل سكربتًا أو ملف أدلة غير موجود.

## قواعد لا تفاوض فيها

- **لا تُضعف رفضًا أمنيًا لتتجاوزه.** لا تحذف الإيصالات أو سجلات العمليات أو تثبيتات الأجيال أو شواهد الإلغاء أو سجلات السحب، ولا تُعد إنشاء حاوية قاعدة بيانات محفوظة. إن كان الرفض خاطئًا، فغيّر القاعدة مع اختبار وسبب مكتوب، كما حدث مع قاعدة قبول المعالج.
- **لا أسرار في أي مكان يمكن أن تنتقل منه.** المجلدان `.secrets/` و`.lab/` مستثنيان، ولا يُقرآن أبدًا إلى السجلات أو الأدلة أو البلاغات أو الإيداعات. كلمات المرور تُقرأ من ملفات بالوضع 0600 أو من stdin، ولا تُمرَّر أبدًا وسائط.
- **ادعاءات صادقة فقط.** لا تدّعِ الجاهزية للإنتاج أو الأمان الكامل أو الاستعادة التلقائية أو عددًا ثابتًا من المشاريع لكل خادم. قل ما شُغّل، وأين، وما لا يثبته.
- **خدمات الأصل كما هي.** تعمل مكونات Supabase بصور أصلية مثبّتة الإصدار، ولا تتغير إلا عبر [سياسة التحديث](docs/engineering/UPSTREAM-UPDATE-POLICY.md). نسخ خدمات الأصل المشتقة وتعديلاتها خارج النطاق.

## الأسلوب

- استخدم bun لكل ما يخص JavaScript (`bun install` و`bun run` و`bun test`)، ولا تستخدم npm أو npx أبدًا؛ واستخدم `/usr/bin/python3` لبيئة تشغيل Python.
- الإنجليزية في الكود والتعليقات ورسائل الإيداع. الوثائق الموجهة للقارئ بالإنجليزية والعربية؛ انظر [docs/engineering/ARABIC-DOCS.md](docs/engineering/ARABIC-DOCS.md). لا شرطات طويلة أو متوسطة في أي مكان؛ استخدم الفواصل أو النقطتين أو جملتين.
- رسائل إيداع تقليدية (conventional commits): `fix(install): ...` و`feat(console): ...` و`docs: ...`. جهّز للإيداع مسارات تغييرك وحدها.
- طابق الكود المحيط: تسمياته، وكثافة تعليقاته، وإيجازه.

## الثغرات الأمنية

لا تفتح بلاغًا عامًا (issue) عن ثغرة. انظر [سياسة الأمان](SECURITY.ar.md).
