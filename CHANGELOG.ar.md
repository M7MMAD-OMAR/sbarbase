[English](CHANGELOG.md)

# سجل التغييرات

كل التغييرات المهمة في صباربيز. يتبع السجل صيغة Keep a Changelog، ويستخدم المشروع Semantic Versioning، وفي إصدار من السلسلة 0.x يعني ذلك أن الواجهة قد تتغير بين إصدار وآخر.

هذا إصدار للكود المصدري. لا شيء هنا منشور كخدمة، ولم تُجرَّب عليه أي تجربة كاملة على خادم، والمستودع يصف نفسه بأنه قيد التطوير. اقرأ قسم "ليس في هذا الإصدار" قبل أن تعدّ أي شيء جاهزًا للإنتاج.

## [لم يُصدر بعد]

### أُضيف

- **رفع ملفات حتى 50 MiB، أو الحد الذي تختاره.** يمر رفع الملفات إلى Storage عبر البوابة ووكيل TLS كما يصل، بدل أن يتوقف عند 1 MiB. يضبط `SBARBASE_UPLOAD_LIMIT_MB` (50 افتراضيًا، كما في Supabase) الحد للبوابة والوكيل وStorage معًا، ويُطبَّق أي تغيير عند التشغيل التالي بإعادة إنشاء حاوية Storage المشتركة. تبقى أجسام الطلبات الأخرى عند 1 MiB.
- **السجلات والمقاييس لكل بيئة.** يرى كل عضو في صفحة البيئة آخر ساعة من الطلبات عبر البوابة: المجاميع، وأخطاء العميل والخادم، والوسيط والنسبة 95 لزمن الاستجابة، والطلبات في كل دقيقة ولكل خدمة، واستهلاك خدماتها الخاصة للذاكرة والمعالج. ويقرأ المالكون والمديرون أيضًا آخر الطلبات وسجلات Auth وREST وStorage وRealtime، مع إخفاء المفاتيح والرموز وكلمات المرور قبل أن يخرج أي سطر من الخادم. العدّ في الذاكرة ويبدأ من جديد بعد إعادة التشغيل.
- **Realtime لكل بيئة.** يشغّل المالكون والمديرون Realtime لبيئة من لوحة الإدارة؛ فيعمل Realtime الأصلي مثبّت الإصدار لتلك البيئة وحدها، بحساب دخول خاص بها، ويتوقف عند إطفائه. يعمل البث والحضور وتغييرات قاعدة البيانات في supabase-js (كل جداول `public`، مصفّاة بأمان الصفوف) عبر البوابة، التي تفحص المفتاح قبل أن يصل أي اتصال إلى Realtime. لا يحمل حساب الدخول صلاحيات المدير إلا أثناء إنشاء Realtime لمخططه، في أول تشغيل وبعد الترقية.
- **إعدادات تسجيل الدخول ومزوّدو OAuth لكل بيئة.** يضبط المالكون والمديرون من اللوحة عنوان الموقع، وعناوين إعادة التوجيه المسموح بها، وإنشاء الحسابات والدخول المجهول، وأيًا من 19 مزوّدًا (Google وGitHub وApple وغيرها). الحفظ يعيد إنشاء Auth تلك البيئة وحدها، ويُبقي السابق إذا لم يعمل الجديد. تصل روابط البريد وخطوات OAuth إلى Auth دون مفتاح، كما في Supabase. يحدد `SBARBASE_PUBLIC_URL` العنوان الذي تستخدمه الروابط وعناوين العودة.
- **الوصول من المتصفح.** تستطيع صفحة على أي نطاق استدعاء Auth وREST وStorage عبر البوابة، كما في Supabase: كل رد يحمل `Access-Control-Allow-Origin: *`، وطلب التحقق المسبق يأخذ 204 دون مفتاح، ولا يوجد وضع لبيانات الاعتماد. يبقى دخول المشغّل مقصورًا على نطاق اللوحة نفسها.
- **ترقيات مع عودة تلقائية.** يتحقق `lab/upgrade.py start` من الإصدار الجديد، ويسحب صوره، ويأخذ نسخة احتياطية من كل بيئة، ثم ينقل نسخة المستودع. إعادة التشغيل تستبدل فقط حاويات Auth وREST وStorage التي تغيّر تثبيتها أو إعدادها. إذا فشل ذلك التشغيل، يعود المشرف وحده إلى الإصدار السابق. يُرفض أي إصدار يغيّر صورة PostgreSQL. يرقّي CI ‏PostgREST من v14.15 إلى v14.16، ثم يعود من إصدار معطوب، على جهاز نظيف.
- **الحصة العادلة في البوابة.** لكل بيئة 8 طلبات مضمونة في الوقت نفسه، ويمكن للمزدحمة أن تستعير الأماكن الفارغة حتى 24 من 32، مع إبقاء الحصة غير المستخدمة لكل بيئة نشطة في الدقيقة الأخيرة فارغة (8 أماكن على الأقل). فُحصت عبر HTTP محلي (`docs/evidence/fair-share-checks.json`)، لا مع Supabase ولا تحت حمل مستمر.
- **تنبيه التشبّع.** إذا بقيت بيئة 15 دقيقة متتالية تُرفض عند حدها، أو تزاحم جارة لها، يصل المشغّل تنبيه واحد من نوع `environment.saturated`. الاستعارة وحدها لا تُطلقه أبدًا.
- **تيليجرام** قناة تنبيه ثالثة بجانب البريد وخطاف الويب.
- **Supabase Studio لكل بيئة.** يشغّل مالكو العميل ومديروه Studio وpostgres-meta الأصليين مثبّتي الإصدار لبيئة واحدة من لوحة الإدارة، ويفتحونه على عنوانه الخاص من خلف تسجيل الدخول إلى اللوحة، ثم يوقفونه. يدخل Studio إلى قاعدة البيانات بحساب يُنشأ مع كل تشغيل، ليس مستخدمًا خارقًا، ولا يوجد إلا أثناء عمله. يستخدم CI عبره الجداول وSQL والمستخدمين والحاويات على جهاز نظيف.
- **تجربة كاملة على خادم فارغ في آلة افتراضية محلية.** يشغّل `lab/vm-rehearsal.sh` آلة Fedora 44 Cloud افتراضية مؤقتة، ويثبّت من نسخة نظيفة من المستودع بأمر القبول الواحد، وينشئ أول مشروع، ثم يعيد التشغيل. نجحت على 4 vCPU و6 GiB (`docs/evidence/vm-empty-server-rehearsal.json`). ليست خادمًا حقيقيًا.
- **فحص المشروع الأول.** الملف `lab/first-project-check.ts`، والخيار `--first-project` في `deploy/server-acceptance.sh`: تسجيل الدخول، والمشروع، والبيئة، والمفتاح، وAuth وREST وStorage عبر البوابة باستخدام supabase-js، ورفض مفتاح ملغى.
- المتغير `SBARBASE_CONSOLE_PORT` يثبّت منفذ لوحة الإدارة على الحلقة المحلية ليستخدمه وكيل TLS.
- تكامل مستمر (CI) لمجموعات اختبارات الوحدات، مع مهمة قبول يدوية على خادم فارغ. تعمل مجموعة Python أيضًا مع عنوان Docker لا يمكن الوصول إليه، فيفشل في المراجعة أي اختبار وحدة يحتاج خدمة Docker.
- **فحص الاستيراد (المرحلة 0).** يقرأ `lab/import_inspect.py` مصدر Supabase للقراءة فقط، ويذكر ما سيرفضه الاستيراد أو يحذّر منه أو يتركه للعمل اليدوي (`docs/evidence/import-inspect-checks.json`). الاستيراد نفسه غير موجود بعد.
- واجهة الإدارة: إنشاء منظمة (لمالكي ومديري منظمة الإعداد الأولي)، وعرض أعضاء منظمة، وإعادة محاولة بيئة فشلت أو أُلغيت. وللوحة الإدارة شاشات مقابلة.
- يسجّل فهرس التحكم إصدار مخططه، ويرفض فهرسًا كتبه إصدار أحدث.
- يسجّل تصدير الاستعادة المنظمة المالكة والمشروع والبيئة.
- أُعيد تنظيم الوثائق في شروح وأدلة ومراجع وقرارات، مع رسوم مرسومة باليد، ودليل بدء سريع، ودليل للخادم، و`SECURITY.md`، ونموذج تهديد، و`CONTRIBUTING.md`، وخارطة طريق.

- **ترحيل جيل الحاوية.** يستبدل `lab/migrate-generation.py` حاوية قاعدة بيانات مُدارة على وحدة تخزين البيانات نفسها، بسجل نية يُثبَّت على القرص بـfsync، ونقطة تحقق لكل مرحلة، وخيار `--reconcile` صريح. نجحت خمس نقاط انهيار بـSIGKILL على البيئة الثابتة المؤقتة (`docs/evidence/fresh-worker-generation-crash-all.json`). يرفض الأمر مكان التشغيل المحفوظ: ذلك التشغيل خطوة مقصودة بحضور المشغّل ولم تحدث بعد، لذا يبقى `lab/durable-check.ts` معطّلًا ويبقى قياسا الحمل المحجوبان محجوبين.

### أُصلح

- ما كشفته التجربة الكاملة على الخادم الفارغ: كان القبول يفحص لوحة الإدارة قبل بنائها. وكانت حدود الإدخال والإخراج تسمّي قسمًا من القرص، والنواة ترفض ذلك، فلم تكن قاعدة البيانات تبدأ أبدًا على تقسيم القرص المعتاد في خوادم VPS. وكانت طلبات POST وDELETE التي بلا جسم تصل بجسم عبر منفذ الاستماع الحقيقي، فأجاب إصدار المفاتيح وإلغاؤها بـ400. وكان سحب الصور يعمل بمهلة 600 ثانية دون أي عرض للتقدم. وكان الفشل في أول تشغيل يُعرض كأنه مصدر محفوظ ينتظر التبنّي.
- كان مالك أو مدير أي منظمة يستطيع قراءة تنبيهات كل المنظمات، بمعرّفاتها وأسباب فشلها. صار كل مستدعٍ يرى أحداث منظماته وحدها.
- كان CI يفشل على خادم نظام ملفاته الجذر قسم من القرص: اختبار ما زال يتوقع القسم، بينما تسمّي طبقة التشغيل القرص كله، وهذا هو الصحيح.
- خمسة اختبارات وحدة للقبول كانت تستدعي Docker فعلًا وتفشل دون خدمته.

### تغيّر

- صار مخطط فهرس التحكم في الإصدار 3: يقبل جدول التسليم قناة تيليجرام. الترحيل باتجاه واحد، فالإصدار الأقدم يرفض فهرسًا فتحه هذا الإصدار.
- صارت بوابة التطبيقات تُعير الأماكن الفارغة بدل رفض كل طلب يتجاوز 8 لكل بيئة، ويبقى فحص الحمل يقيس الحصة الثابتة.
- صار شرط البدء يُشتق من مكان التشغيل الذي يبدؤه الأمر فعلًا (1792 MiB من الحدود على خادم فارغ، و512 MiB لكل بيئة، واحتياطي 2560 MiB)، ويستخدم الفحص المسبق و`ExecStartPre` في الوحدة وطبقة التشغيل حسابًا واحدًا. كان الشرط ثابتًا: 5888 MiB في الفحص المسبق و6 GiB في طبقة التشغيل.
- تُقبل حدود المعالج كسقوف: حتى ضعف عدد الأنوية بعد ترك نواة واحدة للخادم. القاعدة القديمة كانت تطلب 8 أنوية.
- تُرفض البيئة الجديدة إذا كانت إعادة التشغيل التالية لن تستطيع قبولها.
- أسماء المشاريع فريدة داخل المنظمة، وأسماء البيئات فريدة داخل المشروع. التعارض يعيد 409 لا 500. وترفض الواجهة بـ409 أي بيئة تتجاوز حد التثبيت قبل وضعها في الطابور.
- نقل مشروع يلغي مهامه المنتظرة في الطابور أثناء النقل، ويوجّه مهامه إلى الوجهة، فتذكر الأحداث اللاحقة المالكين الجدد.

- أداتا الحمل لم تعودا تكتبان فوق أدلتهما عند الفشل، ولا تتركان التنظيف يخفي الخطأ الحقيقي. وصارت أداة SDK تفحص بيئتها الثابتة أولًا.
- لم يعد `NOTICE` يصف Supabase Studio بأنه مُقدَّم، فهو مخطط له.

### ملاحظات

- يوجد في المستودع ملف `LICENSE` (Apache-2.0). يقول مدخل 0.1.0 أدناه إنه لا يوجد ملف ترخيص، وأبقينا ذلك المدخل كما صدر.

## الإصدار [0.1.0]، 2026-09-21

أول محطة موسومة. تحمل عمل المنصة على العزل والمحاسبة، وبريد كل بيئة، وتنبيهات المشغّل، وأدلة القياس التي تقول ما يفعله العزل وما لا يفعله.

### أُضيف

- **فئات الموارد وفصل الإدخال والإخراج على القرص.** تُشغَّل كل حاوية ضمن فئة سياسة لها وزن للمعالج، ووزن للإدخال والإخراج، وحدود لكل جهاز لسرعة القراءة والكتابة وعدد العمليات في الثانية (IOPS). يُستخرج الجهاز من الخادم بدل كتابته يدويًا، ويرفض التشغيل بدل أن يبدأ حاوية بلا فصل للإدخال والإخراج. `lab/resource_policy.py`، `docs/engineering/RESOURCE-POLICY.md`.
- **مكان تشغيل معدود يُستخرج من خدمة Docker.** يقرأ القبول الحاويات التي تنسبها خدمة Docker إلى وسم المالك، بدل قائمة أسماء مكتوبة يدويًا. فالمكوّن الذي لم يُخبر به أحدٌ الوحدةَ يُعدّ مع ذلك، والحاوية الموسومة خارج مكان التشغيل المسجّل سبب للرفض ما دامت تعمل.
- **أخذ عينات مستمر لضغط الخادم واستجابة من المستوى 1.** يأخذ `lab/pressure_admission.py` سلسلة محدودة من القراءات خلال نافذة زمنية ويلخّصها، ويرفض القبول الجديد ما دامت أحدث قراءة عند العتبة أو فوقها، ويضيف كل تجاوز إلى دفتر دائم. كل قرار يذكر، بجانب الإجراء، ما لم يُنفَّذ بعد.
- **بريد كل بيئة.** ملف إعدادات واحد بصلاحية 0600 لكل بيئة، ومتغيرات بيئة لـAuth تُبنى منه، وأمر مطابقة صريح يعيد إنشاء Auth لبيئة واحدة ليطبّق الإعدادات أو يزيلها، وملف حالة غير سري. `lab/mail_config.py`، `lab/mail_state.py`، `docs/engineering/ENVIRONMENT-EMAIL.md`.
- **تنبيهات المشغّل.** صندوق صادر دائم وجداول تسليم تُثبَّت صفوفها داخل المعاملة نفسها التي تسجّل تغيير الحالة الذي تصفه، وتصريف داخل العامل، وقناة بريد، وقناة خطاف ويب موقّع، وبوابة تنقية لبيانات الاعتماد تفشل مغلقة، وأحد عشر نوعًا من الأحداث موصولة عند تغييرات حالتها الدائمة (بدء التثبيت وإيقافه وفشل بدئه، وإعادة تشغيل العامل وبلوغ حد إعادة التشغيل، وتطبيق السياج ورفعه، واكتمال التصدير وفشله، والتحقق من الاستعادة وفشلها). `lab/notify.py`، `lab/notification_producers.py`، `docs/engineering/OPERATOR-NOTIFICATIONS.md`.
- **واجهات في لوحة الإدارة للاثنين.** مسار للقراءة فقط يعرض حالة بريد البيئة، وآخر يعرض عدد التنبيهات غير المسلّمة. تظهر حالة البريد في صفحة البيئة، والعدد في إطار لوحة الإدارة.
- **فحوص حية مؤقتة مع ضوابط سلبية.** فحص بريد يثبت التسليم، ويختبر بيئة غير مُعدّة، وخادم ترحيل معطّلًا، وبيانات اعتماد خاطئة. وفحص تنبيهات يثبت التسليم، ومنع التكرار، وبوابة التنقية، وأن القناة المعطّلة تسجّل فشلًا بدل أن تغيّر العملية التي تبلّغ عنها.
- **الوثائق.** ثلاثة سجلات تصميم، ومراجعة نقدية تحدد معايير قبول لا يمكن تزييفها، وخطة تنفيذ، وخطة لترحيل جيل الحاوية المؤجل، وأدلة القياس في `docs/evidence/`.
- عرض Studio الأصلي لكل بيئة على مسار الإدارة، وصارت لوحة الإدارة تتبع سمة نظام التشغيل افتراضيًا.

### تغيّر

- صار حساب مكان التشغيل مشتقًا بدل أن يكون قائمة، وصفوف الإدخال والإخراج هي الآلية التي تفرض الحد فعلًا، لأن القياس أظهر أن عمود الوزن لا يفرضه.
- أُزيل جدول الفهرس `environment_mail` الذي لم يكتب فيه أحد، واستُعيض عنه بملف الملخص الذي تكتبه طبقة التشغيل نفسها، وهو المصدر الوحيد الذي تقرؤه لوحة الإدارة.
- صارت مقارنة إعدادات البريد تجري في الاتجاهين، فلا يبقى في حاوية معاد استخدامها مفتاح لم يعد في الإعدادات المطلوبة.

### أُصلح

- البريد: كان مسار "بلا تغيير" يسجّل حالة لا يعرّفها المعجم، فكان التشغيل الثاني على إعدادات لم تتغير يرمي خطأ بدل أن يبلّغ. صار اختبار على مستوى المصدر يمر على كل موضع يُستدعى فيه التسجيل ويشترط حالة معرّفة.
- البريد: كانت الإعدادات التي حذفها المشغّل قد تنجو من إعادة التشغيل، وتُبقي في الحاوية المعاد استخدامها بيانات اعتماد SMTP حية وحدود المعدل وفترة تهدئة خادم الترحيل.
- التنبيهات: كانت نافذة التصعيد هي نافذة الخطورة نفسها، فكانت القناة المعطّلة ستُصعّد نحو 72 مرة كل ست ساعات بدل مرة واحدة، وكان يمكن لقناتين معطّلتين أن تصعّد كل منهما الأخرى.
- مكان التشغيل: كانت حاوية مملوكة خارج مكان التشغيل المسجّل ترفض بدء التثبيت، بينما كانت المراجعة السابقة تقبلها. وكانت حاوية موسومة تحت أي مالك آخر تفلت من العدّ، مع أن وثائق الوحدة نفسها كانت تزعم أنها معدودة.
- مكان التشغيل: صار كل موضع ينشئ هدف استعادة يحمل وسم فئته، والحارس الذي كان يزعم أن كل موضع تشغيل يمرّر فئة صار حدًا أدنى، لا عدًّا دقيقًا ينكسر عند إضافة موضع تشغيل جديد.
- فحص التنبيهات: صار تشغيله الافتراضي يختبر بوابة التنقية التي تفشل مغلقة، ولم يكن يفعل. ولم تعد مقارنة الملخص فيه تعيد قراءة جملة وقت العرض التي كانت تجعل أحد الفحوص رمية عملة.
- البريد: يُرفض الرابط الرمزي المعلّق بدل أن يُقرأ بصمت كأن البيئة غير مُعدّة.

### ما قيس، مع أدلته في المستودع

- الملف `docs/evidence/resource-policy-cgroup-mapping.json`: يتحوّل `--cpu-shares` إلى `cpu.weight` في cgroup v2 بعلاقة أقل من الخطية (القيمة 2048 تعطي 174 لا 800)، ولم يفرض `--blkio-weight` شيئًا على هذا الخادم، بينما فرض `--device-write-bps` الحد، ووضع قيمة `wbps` رقمية في `io.max`.
- الملف `docs/evidence/noisy-neighbor-sql-runs.json`: خمسة تشغيلات لتجربة الجار المزعج، حُفظ منها ثلاثة. لا أثر قابل للتكرار: الوسيط والشريحة p95 والحد الأقصى تبقى ثابتة بين خط الأساس والمرحلة التي تشبع فيها بيئة ثانية المحرك المشترك. أظهر تشغيل سابق قفزة في الذيل لم تكررها أربعة تشغيلات لاحقة، فسُجّلت كقيمة شاذة لا كثمن للجار المزعج.
- الملف `docs/evidence/pressure-response-checks.json`: تجاوز حقيقي للعتبة على حاوية مؤقتة، ورفض المستوى 1، وسطر واحد في الدفتر الدائم، وعودة إلى القبول بعد التعافي.
- الملف `docs/evidence/auth-templates-source-v2.196.0.json`: ما يفعله متغير القالب في إصدار Auth المثبّت، مقروءًا من مصدر الوسم نفسه مع بصمة لكل ملف. قيمة القالب عنوان HTTP، وليست ملفًا أبدًا.
- فحوص حية أعاد المشرف تشغيلها على الشجرة المدمجة: البريد 36 فحصًا، والتنبيهات 21 فحصًا، وكلاهما ينظّف ما أنشأه. البوابات: 575 اختبار Python، و87 اختبار Bun، وفحص أنواع الواجهة، وفحص بناء لوحة الإدارة.

### ليس في هذا الإصدار

نذكره هنا بدل أن يُكتشف لاحقًا:

- **لا تجربة كاملة على خادم.** مصفوفة جاهزية النشر منفّذة ومثبتة على محطة عمل، وليست تجربة على عتاد حقيقي.
- **ترحيل جيل الحاوية مؤجل**، فلا يمكن إعادة إنشاء حاوية قاعدة بيانات محفوظة. لذلك ينطبق عقد الفئات على أماكن التشغيل التي تُنشأ بعده، ويبقى مكان التشغيل المحفوظ على جهاز التطوير مستثنى. الخطة في `docs/engineering/CONTAINER-GENERATION-MIGRATION.md`.
- **قياسان محجوبان، لا مجرد أنهما لم يُشغَّلا**: تجربة الضغط المدفوعة بتوالي الطلبات، وحمل SDK المختلط. مولّد بيئتهما الثابتة الوحيد، `lab/durable-check.ts`، معطّل حتى ذلك الترحيل، وبيئته الثابتة على جهاز التطوير تشير إلى بيئة متقاعدة. وإحدى أداتي الحمل تكتب فوق أدلتها حين تفشل، وتنظيفها يخفي الخطأ الحقيقي.
- **جداول الفئات غير معايَرة.** صفوف الإدخال والإخراج تفرض الحد وتفصل الفئات، ولم تقس أي تجربة ما تحصل عليه الفئة تحت الحمل.
- **ثلاثة أنواع من التنبيهات لا يطلقها أحد**: الضغط، ورفض طبقة التشغيل، وتغيّر الهامش. ولا شيء يستدعي استجابة الضغط وفق جدول زمني.
- **البريد للقراءة فقط في لوحة الإدارة**، وما زال المشغّل يختار مزود SMTP بنفسه. القوالب هي الافتراضية في الأصل، والقالب البديل يجب أن يُخدم من داخل شبكة التشغيل الداخلية.
- **لا ملف ترخيص.** يذكر `NOTICE` حقوق النشر ونسبة الأعمال إلى أصولها، ومنها أن Supabase Studio وpostgres-meta مستخدمان كصور مثبّتة الإصدار دون تعديل، وأن لوحة الإدارة عمل صباربيز الخاص. اختيار الترخيص قرار المشرف، ولم يُتخذ هنا.
