[English](SECURITY.md)

# سياسة الأمان

صباربيز يشغّل مشاريع Supabase كثيرة على خادم واحد، تنسخها احتياطيًا وتستعيدها وترقّيها دون خوف. المشروع قيد التطوير وليس جاهزًا للإنتاج. تبيّن هذه الصفحة أي الإصدارات تتلقى إصلاحات أمنية، وكيف تبلّغ عن مشكلة بسرية، وما يُعد ثغرة هنا، وكيف يحصّن المشغّل خادمه اليوم. المنطق وراءها في [نموذج التهديد](docs/explain/threat-model.ar.md).

## الإصدارات المدعومة

| الإصدار | الحالة | الإصلاحات الأمنية |
| --- | --- | --- |
| 0.1.x | قيد التطوير | تُصلح على `main` حسب الإمكان؛ لا نقل للإصلاحات إلى إصدارات سابقة، ولا دعم للإنتاج |
| ما قبله | لم يُصدر | لا شيء |

لا دعم للإنتاج في أي إصدار. ينزل الإصلاح على `main` وفي مدخل إصدار 0.1.x التالي في [سجل التغييرات](CHANGELOG.ar.md).

## الإبلاغ عن ثغرة

أبلغ بسرية عبر GitHub: افتح [صفحة التنبيهات الأمنية في github.com/M7MMAD-OMAR/sbarbase](https://github.com/M7MMAD-OMAR/sbarbase/security/advisories/new) واختر **Report a vulnerability**. يبقى البلاغ ظاهرًا لك وللمشرفين فقط حتى نشر التنبيه.

إن لم يظهر ذلك الزر، افتح بلاغًا عامًا (issue) تذكر فيه فقط أن لديك ثغرة وتطلب قناة خاصة. لا تضع التفاصيل ولا كود إثبات المفهوم ولا السجلات ولا بيانات الاعتماد في بلاغ عام أو طلب دمج أو نقاش.

البلاغ المفيد يذكر:

- الإيداع أو الإصدار الذي اختبرته؛
- الحد الذي تجاوزته (انظر النطاق أدناه) وما يحتاجه المهاجم ليبدأ؛
- خطوات إعادة الإنتاج، ويُفضّل على المختبر المحلي ([دليل المختبر المحلي](docs/guides/local-lab.ar.md))؛
- ما توقعته وما حدث فعلًا.

لا تُدرج أبدًا بيانات تطبيق حقيقية أو بيانات اعتماد حقيقية أو بيانات شخص آخر. اختبر فقط على تثبيت تملكه.

## ما الذي تتوقعه

يعتني بصباربيز فريق صغير، فهذه نوايا وليست التزامًا بمستوى خدمة:

- إقرار بالاستلام، عادة خلال أسبوع؛
- تقييم لكون البلاغ ضمن النطاق ولمدى خطورته؛
- إصلاح على `main` متى أمكن، مع نسبة الفضل إليك في التنبيه إلا إن طلبت غير ذلك؛
- إفصاح منسّق: أمهلنا حتى 90 يومًا قبل النشر، أو أخبرنا إن احتجت جدولًا زمنيًا مختلفًا.

## النطاق

صباربيز يثق بمن يشغّلون الخادم، ويعامل زوار التطبيقات المستضافة على أنهم غير موثوقين. يكون البلاغ ضمن النطاق إذا مكّن أحدًا من تجاوز حد يقول التصميم إنه يجب أن يصمد.

**ضمن النطاق**

- **الوصول بين البيئات.** أي شيء يسمح لرمز أو مفتاح أو حساب دخول أو طلب يخص بيئة بقراءة بيانات بيئة أخرى أو ملفاتها أو إعداداتها، أو بتغييرها.
- **تسرّب المفاتيح.** كشف المفاتيح العامة الخام أو كلمات مرور الخدمات أو أسرار توقيع Auth أو مفاتيح توقيع Storage عبر واجهة الإدارة أو البوابة أو قوائم المفاتيح أو رسائل الخطأ أو ملفات غير خاصة.
- **تجاوز البوابة.** الوصول إلى Auth أو PostgREST أو Storage لبيئة ما دون مفتاح صالح لها، أو اختيار خادم الأصل أو مستأجر Storage من مدخلات العميل، أو تخطي حدود حجم الطلب أو المهلة أو القبول بطريقة تؤثر في بيئات أخرى.
- **رفع الصلاحيات من حساب دخول خدمة في بيئة.** اتصال حساب Auth أو REST أو Storage لبيئة ما بقاعدة بيانات أخرى، أو تقمّصه دور مدير العنقود، أو تغييره قواعد الاتصال.
- **الخلط في هوية الإدارة.** قبول واجهة الإدارة لرمز مستخدم تطبيق، أو أخذ هوية الفاعل الإداري من ترويسة أو من جسم الطلب بدل المصادقة.
- **كشف الأسرار في السجلات أو الأدلة.** كتابة بيانات الاعتماد أو الرموز أو المسارات الخاصة في السجلات أو مخرجات الأوامر أو الإشعارات أو الملفات المُدارة بالإصدارات تحت `docs/evidence/`.
- **وكيل TLS المرجعي** (`deploy/console-tls-proxy.ts`): إعادة التوجيه المفتوحة، أو حقن ترويسة Host، أو تسجيل أجسام الطلبات أو سلاسل الاستعلام أو ملفات تعريف الارتباط أو بيانات الاعتماد.

**خارج النطاق**

- **مشغّل خادم خبيث.** من يملك صلاحية root أو وصولًا إلى Docker أو حساب مدير PostgreSQL أو الملفات تحت `.secrets/` و`.lab/` يستطيع قراءة كل شيء وتغييره بحكم التصميم. ويستطيع المشغّلون أيضًا تنفيذ أي SQL في أي بيئة بحكم التصميم.
- المكونات التي لم تُبنَ بعد: Realtime وEdge Functions ومجمّع الاتصالات وcron وStudio لكل بيئة.
- الثغرات في خدمات Supabase الأصلية أو PostgreSQL أو الاعتماديات الأخرى، إلا إذا كانت إعدادات صباربيز لها هي سبب المشكلة. أبلغ عنها لأصحابها.
- استنزاف الموارد الذي يتطلب وصول مشغّل موثوق، أو الذي لا يؤثر إلا في البيئة التي يتحكم فيها المهاجم أصلًا.
- النتائج التي تعتمد على تجاهل قائمة التحصين أدناه، كمنفذ قاعدة بيانات مكشوف للإنترنت.
- مخرجات الماسحات الآلية دون أثر مُثبت.

[الثغرات المعروفة](docs/explain/threat-model.md#known-gaps) في نموذج التهديد معلنة أصلًا. ومع ذلك نرحّب ببلاغ يبيّن أن إحداها أسوأ مما وُصف، أو يقترح إصلاحًا لها.

## قائمة التحصين للمشغّلين

هذه الخطوات تقلل الخطر، ولا تجعل التثبيت آمنًا تمامًا. دليل التشغيل هو [دليل النشر على خادم](docs/guides/server-deployment.ar.md).

- **الجدار الناري.** اسمح بـHTTPS الوارد على 443 (وعلى 80 فقط إن أبقيت التحويل إلى HTTPS)، ومعه مسار SSH الذي تدير منه. احجب كل ما عداها. المثبّت لا ينشر أي منفذ Docker، فأبقِه كذلك. يستمع الوكيل المرجعي افتراضيًا على 8443 و8080، فإما أن تمرّر `--https-port 443 --http-port 80` (ربط المنافذ تحت 1024 يحتاج root أو `CAP_NET_BIND_SERVICE`) وإما أن تحوّل هذه المنافذ في الجدار الناري.
- **لوحة الإدارة خلف TLS.** تستمع لوحة الإدارة وواجهة الإدارة والبوابة على الواجهة المحلية فقط. لا تصل إليها إلا عبر وكيل ينهي TLS، إما `deploy/console-tls-proxy.ts` وإما وكيلك الخاص بالسلوك نفسه. لا تكشف أبدًا نقطة Auth الخاصة بالإدارة أو واجهة التجهيز مباشرة.
- **أسرار خاصة.** اجعل `.secrets/` و`.lab/` ومفتاح الشهادة مقروءة لحساب الخدمة وحده (ملفات بالوضع `0600` في مجلد خاص). يرفض الوكيل المرجعي مفتاحًا تقرؤه المجموعة أو الجميع. لا تلصق الأسرار أبدًا في المسائل أو السجلات أو ملفات الأدلة.
- **عامل حساب الخدمة معاملة root.** يعمل `deploy/sbarbase.service` بمستخدم مخصص اسمه `sbarbase` له وصول إلى Docker، وهذا يعادل root على الخادم. لا تسجّل الدخول به ولا تشاركه مع برامج أخرى.
- **نسخ احتياطية مشفّرة خارج الخادم.** صباربيز لا ينسخ النسخ الاحتياطية خارج الخادم. رتّب بنفسك نسخًا مشفّرة على جهاز آخر، واحفظ مواد فك التشفير في مكان يبقى إن ضاع الخادم، وتدرّب على الاستعادة. انظر [النسخ الاحتياطي والاستعادة](docs/guides/backup-and-restore.ar.md).
- **صور مثبّتة الإصدار.** لا تشغّل إلا بصمات الصور الموجودة في `lab/images.lock.json` و`lab/storage-image.lock.json` و`lab/distro-image.lock.json`. يرفض الفحص المسبق عند الإقلاع تثبيتًا مفقودًا أو غير مطابق؛ لا تلتفّ عليه.
- **التحديثات.** غيّر مكوّنًا أصليًا واحدًا في كل مرة كما في [الترقيات](docs/guides/upgrades.ar.md) و[سياسة تحديث الأصل](docs/engineering/UPSTREAM-UPDATE-POLICY.md). سجّل تثبيت التراجع أولًا، واقرأ سجل تغييرات الأصل بحثًا عن إصلاحات أمنية، وشغّل مجموعات الاختبار كاملة بعد ذلك. طبّق تحديثات الأمان لنظام التشغيل ولـDocker على جدولها الخاص.
