# Deployment readiness

Assessment 2026-09-20. One page answering "can this be deployed and tested on a
server, and what is proven versus assumed". Every claim links to its evidence
file. Nothing here is a production capacity claim.

## Readiness matrix

| Requirement | Status | Evidence or gap |
|---|---|---|
| Installation driver starts the runtime it installs | **fixed and regression-tested** | Running the documented acceptance command on this host exposed a defect the reviews had not: `install()` held the operation lock while spawning `lab/installation_runtime.py up`, which takes the same lock for its own lifetime, so the installer refused its own runtime on every host. The lock is now released after the console-build step and the runtime start is serialised by the runtime itself (`lab/test_install_lock.py`, 5 tests: the lock is free when the runtime starts, a refused start stops the install, a failed step still releases the lock, a second installer is refused) |
| One-command server acceptance with evidence | implemented, refusals tested | `deploy/server-acceptance.sh`: prerequisites, preflight, rehearsal, handoff copy of the evidence; 10 tests in `lab/test_server_acceptance.py` cover a missing prerequisite, a public bootstrap file, unknown arguments, no secret leakage, a failed step stopping the run, and the unit being released before the rehearsal and restored after |
| Host preflight (Docker native, Bun, `/usr/bin/python3` 3.14+, pinned images, headroom, disk, state) | implemented, proven | `lab/install_server.py check`. What it reports depends on the moment on a shared host: measured here as `Host headroom insufficient: 8660 MiB available, plan needs 8761 MiB (5888 MiB placement + 2560 MiB reserve + 313 MiB measured for the running source stage)`, and at another moment as `0 blocker(s), 2 action(s)` with the same plan. The composition is spelled out in the blocker, including the measured cost of the running source stage, so a combined start refuses before creating containers instead of dying halfway. Also reported: the retained installation (11 source, 8 target containers), the current recovery target's generation pin, and a historical target without one that the runtime does not start |
| Supervisor unit rendering, verification and install | rendering and verification **passed on this host**; the install needs root on the server | `lab/install_server.py supervise`: the shipped unit is rewritten for the installation's paths, service user and Bun directory, the result is verified with `systemd-analyze verify`, and the exact install commands are recorded (`docs/evidence/supervisor-unit.json`, `passed: true`). It refuses to rewrite a unit whose directives moved, refuses an empty Bun directory, refuses an account or path that is not plainly what it claims to be, refuses to install a unit that does not verify, refuses `--apply` without root, and fails the run if the installed unit never becomes active. `--apply` installs to `/etc/systemd/system/sbarbase.service`, reloads, enables and starts it |
| Supervised path under systemd | **passed on this host**, 10 checks | `docs/evidence/supervised-run.json`, 17:08, 10 of 10: a temporary systemd user unit mirroring the shipped unit's directives (`ExecStartPre` gate, `ExecStart`, PATH) was loaded, accepted the start, started the supervisor and the console, the console and management realm answered, owned containers were started by the unit, the preflight gate ran under systemd, `systemctl stop` returned cleanly, the unit file was removed and no owned container was left running. Not the shipped unit installed at `/etc/systemd/system`, which the server acceptance run covers (`--require-unit`). This run satisfies the tightened check: the gate row requires the gate's own `Preflight: 0 blocker(s)` line, not any gate output, and the console wait accepts state written after the start under systemd only, not state left by an earlier run |
| Full source and target lifecycle rehearsal | **passed on this host** | `docs/evidence/deployment-rehearsal.json`, 11 of 11 checks, 2026-09-20 17:21: preflight, built page intact, supervisor start, console serving, management identity realm 200, all 8 environment routes 200, 18 live operator bootstrap checks, 14 combined gateway checks, clean shutdown, no owned container left running. The evidence also carries the startup context (`attempts_used: 1`, no refusals) and the unit state it saw (`verify: passed` against the checkout's template, `verify_source: template`, since no unit is installed here). The supervised-path row in that run is green because it was not required: the supervisor was started directly and `sbarbase.service` was not installed, so nothing under systemd was exercised by it. The acceptance variant (`--require-unit`, own evidence file) requires the shipped unit to be installed, which needs root on the server |
| Fresh install from an empty host | every step proven in isolation, the empty-host run has not happened | the steps are separately evidenced on this host: private directories guarded, all 5 pins present and digest-verified, console built and its page verified, the owned runtime started by the rehearsal, the operator bootstrap passing 18 live checks, and supervision under systemd. An empty-host run needs a host with no retained installation, because placement container names are fixed |
| Combined runtime admission on this host | passed | `docs/evidence/combined-runtime-admission.json`: 5888 MiB of container limits and 5.75 CPUs admitted at 8943 MiB available, target started in the same run. The rehearsal that followed recorded 8885 MiB available (`docs/evidence/deployment-rehearsal.json`), and the 17:21 rehearsal 8481 MiB |
| HTTPS termination | **passed on this host**, 21 checks | `lab/tls_termination_check.py`: `deploy/console-tls-proxy.ts` terminates TLS (self-signed certificate in the check) in front of a stub serving the real built page; the page and its assets arrive byte for byte over HTTPS, a management route is reachable, HSTS and nosniff are set, the protocol is forwarded as https, plain HTTP gets a 308, a client supplied `Host` header cannot override the configured public host or the redirect target, hop by hop headers are not forwarded, a declared body over `--max-body` is answered 413 without being read, and the proxy refuses a world-readable key, a non-loopback upstream, a missing certificate, a missing public host and a public host that is not a bare host name (`docs/evidence/tls-termination.json`). A public certificate and the operator's own proxy are the server's business |
| Operator bootstrap step, live | **passed on this host**, 18 checks | the rehearsal runs `bun lab/bootstrap-check.ts` against the live management Auth and records `18 live operator bootstrap checks passed`; it covers real Auth operator creation, interrupted bootstrap recovery, password verification, the private journal, idempotent catalog and authenticated organization discovery, and cleans its probe account up. The script writes its own artifact to `.lab/upstream/bootstrap-verification.json`; `docs/evidence/bootstrap-checks.json` is a copy of an earlier, separate run of the same script, kept for the record |
| Console build step, isolated and verified | proven on this host | `lab/console_build_check.py`: build exit 0, page 392 bytes with both referenced assets present and hashed in `docs/evidence/console-build.json`; 8 tests in `lab/test_console_build.py` |
| Console static-serving layer, isolated | proven on this host, 17 checks | `bun lab/console-serve-check.ts`: the built page and both assets are served on an ephemeral loopback port, served asset bytes match the built files, and the security headers, method refusal and loopback host check all hold. The traversal checks send the literal `GET /../src/...` and a percent-encoded variant over a raw socket, because `fetch` normalises a traversal path before it leaves the process; a third check exercises the asset allow-list directly and proves it still admits a real asset (`docs/evidence/console-serve.json`) |
| Pinned images present and matching their digests | proven on this host | `lab/pinned_images_check.py`: all 5 pins resolve to the pinned digest in `docs/evidence/pinned-images.json`; 6 tests in `lab/test_pinned_images.py`. The installer now re-verifies every pin after ensuring presence |
| Retained installation migrated to owned HBA authority | implemented and executed | both retained databases adopted; per role an operation section (7 checks) and a verification section (12 checks) in `docs/evidence/retained-source-adoption.json` and `retained-target-adoption.json`, rules preserved byte for byte |
| Recovery target placement starts and serves | rehearsed live, passed | `docs/evidence/target-placement-rehearsal.json`, 12 checks: lifecycle start, Auth and REST 200, routing resumed, clean stop |
| Fresh-target HBA writer on a disposable container | proven, 16 checks | `docs/evidence/target-hba-creation-checks.json`; the consuming restore path has not been re-run with the owned writer |
| Adversarial review of the target and deployment code | completed, 12 must-fix findings all fixed | `docs/reviews/target-and-deployment-review.md`; fixes covered below |
| Second adversarial review of the deployment tooling | completed, 20 must-fix findings addressed | `docs/reviews/deployment-tooling-review-2.md`; it also falsified 17 claims in the status documents, all corrected here. The remaining items are recorded as open, not fixed: a cosmetic `rehearse()` return value, and the check-count totals that only a fresh full rehearsal can re-derive |
| Deployment rehearsal in one command | implemented, records refusal honestly | `lab/deployment_rehearsal.py`; the committed run passed 11 of 11 on 2026-09-20 17:21 (`docs/evidence/deployment-rehearsal.json`). A run while the host is short of memory records the refusal and starts nothing, measured repeatedly here: `Combined resource admission refused: host_memory_headroom` at 8736 MiB and 8488 MiB available against a 8761 MiB plan, and the green run succeeded once the host reached 8900 MiB. A run that cannot install records `installation steps completed` false with the diagnostic path instead of dying without evidence, and `deploy/server-acceptance.sh` says whether the evidence file was written |
| Server acceptance run as documented | **12 of 12 checks green on this host** | `deploy/server-acceptance.sh --rehearse` runs prerequisites, preflight, console static serving, TLS termination, unit rendering, installation (state, pins, console build, owned runtime start), then the rehearsal with the unit required. With the shipped unit installed as root (the one step that needs root) the rehearsal is green in full: `docs/evidence/server-acceptance-rehearsal.json`, 12 of 12 at 18:08, including `supervisor unit is enabled on this host` and `the unit systemd runs verifies` against `/etc/systemd/system/sbarbase.service` itself, 18 live bootstrap checks, 14 combined gateway checks and no owned container left running. Earlier on the same host the same run recorded 11 of 12 with only that unit row red, which is what the root-only step adds |
| Supervision with a restart policy | **installed, enabled and active on this host**; two preconditions were defects, now fixed | `deploy/sbarbase.service` with `ExecStartPre` preflight, a restart policy and a 150 s stop budget. Installing and starting it for real exposed two blockers no review had found: the unit demanded `ReadWritePaths=$HOME/.secrets`, a directory nothing creates, so systemd failed it with `226/NAMESPACE` before the first command ran (the unit now names the checkout only, and a test refuses any write path outside it); and a system service does not inherit the operator's shell, so on a host whose docker context points at a desktop socket the daemon is unreachable and the preflight now says exactly that, naming the endpoint it tried, instead of reporting the images as absent and the host as a fresh install. With the checkout path fixed and `DOCKER_HOST` pointed at the system socket, the unit installed, enabled and reached `active` here (17:59) |
| Account and layout the installation runs as | named, validated, refuses a missing account | `--service-user`, `--home` and `--bun-dir` are accepted by `lab/install_server.py supervise` and forwarded by `deploy/server-acceptance.sh`; the installer refuses `--apply` for an account that does not exist (the shipped default is `sbarbase`) and records `service_account: present|missing` in its evidence, so an install never produces a unit that cannot start |
| HTTPS and network exposure | termination implemented and checked on this host; the public certificate and the production proxy are the operator's | `deploy/console-tls-proxy.ts` and `lab/tls_termination_check.py` (21 checks, `docs/evidence/tls-termination.json`); runbook in `docs/SERVER-DEPLOYMENT.md`. No port is published by the installation itself |
| Upgrade and rollback | policy plus runbook, no release adopted yet | `docs/UPSTREAM-UPDATE-POLICY.md`, `docs/SERVER-DEPLOYMENT.md` |
| Backup and restore | restore evidenced locally, off-host not implemented | `docs/INDEPENDENT-RESTORE.md`; off-host restore remains out of scope |
| Capacity at 10 or 100 projects | not claimed | configured ceilings only, no measured peak demand |
| Realtime, Functions, pooler, cron | not implemented | out of scope for this revision |

## Rehearsal order once a server exists

1. `/usr/bin/python3 lab/install_server.py check` on the server. Fix every
   blocker first; a host that fails validation must not be half-installed.
2. `/usr/bin/python3 lab/deployment_rehearsal.py --bootstrap-file <0600 JSON>`
   for a fresh host. It installs, supervises, verifies console and management
   reachability, probes every recorded environment route, shuts the supervisor
   down and asserts no owned container is left running, writing
   `docs/evidence/deployment-rehearsal.json`.
3. `/usr/bin/python3 lab/target_placement_rehearsal.py` when the installation
   has a recovery target, to re-prove that placement on the server.
4. `bun lab/combined-supervisor-check.ts` for the full source plus target
   rehearsal, which needs the full headroom.
5. Keep the evidence files with the deployment record. A rehearsal that fails
   is evidence too: the refusal is recorded, not hidden.

## Honest gaps that remain after a successful server rehearsal

- Sustained mixed load, connection saturation and 10/100-project capacity are
  still unmeasured; a short rehearsal proves availability, not capacity.
- Controller death during active provisioning, power loss and multi-host
  coordination remain unproven; the current crash evidence covers bounded
  checkpoints, not arbitrary crashes.
- The storage verification probe and the legacy bootstrap scripts still write
  `pg_hba.conf` directly, as inventoried in `docs/TARGET-HBA-WRITERS.md`. They
  are check-time or legacy paths, not managed runtime writers.
- The supervised unit needs a daemon socket the service can reach: on a server
  with native Docker that is `/var/run/docker.sock` and the `docker` group; on a
  host whose docker context points at a desktop socket it needs the drop-in noted
  in the runbook. The unit reached `active` here only after that drop-in was
  added.
- Container-generation migration has a design only
  (`docs/CONTAINER-GENERATION-MIGRATION.md`).
- The bootstrap flow has no invitations, MFA or rate limiting, and no
  supported repair or reset procedure.