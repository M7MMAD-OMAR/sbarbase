{
  "scope": "Fresh separate PostgreSQL cluster, database stage only. No target Auth/REST/Storage processes, object restore or signed-URL verification yet. Source stayed stopped; target stopped with isolated volume retained.",
  "checks": [
    "fresh destination volume has disk and inode headroom",
    "fresh pinned Supabase cluster ready",
    "source remains stopped during target bootstrap",
    "canonical API defaults match source",
    "logical dump restored transactionally on separate cluster",
    "complete restored roles matches encrypted export",
    "complete restored memberships matches encrypted export",
    "complete restored database_acl matches encrypted export",
    "complete restored settings matches encrypted export",
    "application Auth and Storage table inventory matches",
    "restored table row count and digest match auth.audit_log_entries",
    "restored table row count and digest match auth.custom_oauth_providers",
    "restored table row count and digest match auth.flow_state",
    "restored table row count and digest match auth.identities",
    "restored table row count and digest match auth.instances",
    "restored table row count and digest match auth.mfa_amr_claims",
    "restored table row count and digest match auth.mfa_challenges",
    "restored table row count and digest match auth.mfa_factors",
    "restored table row count and digest match auth.oauth_authorizations",
    "restored table row count and digest match auth.oauth_client_states",
    "restored table row count and digest match auth.oauth_clients",
    "restored table row count and digest match auth.oauth_consents",
    "restored table row count and digest match auth.one_time_tokens",
    "restored table row count and digest match auth.refresh_tokens",
    "restored table row count and digest match auth.saml_providers",
    "restored table row count and digest match auth.saml_relay_states",
    "restored table row count and digest match auth.schema_migrations",
    "restored table row count and digest match auth.sessions",
    "restored table row count and digest match auth.sso_domains",
    "restored table row count and digest match auth.sso_providers",
    "restored table row count and digest match auth.users",
    "restored table row count and digest match auth.webauthn_challenges",
    "restored table row count and digest match auth.webauthn_credentials",
    "restored table row count and digest match public.durable_items",
    "restored table row count and digest match storage.buckets",
    "restored table row count and digest match storage.buckets_analytics",
    "restored table row count and digest match storage.buckets_vectors",
    "restored table row count and digest match storage.migrations",
    "restored table row count and digest match storage.objects",
    "restored table row count and digest match storage.s3_multipart_uploads",
    "restored table row count and digest match storage.s3_multipart_uploads_parts",
    "restored table row count and digest match storage.vector_indexes",
    "database locale ownership and limits match",
    "actual collation version matches recorded version",
    "restored auth credential connects only to target",
    "Auth search path restored",
    "restored rest credential connects only to target",
    "REST SQL deadlines apply after fresh login",
    "restored storage credential connects only to target"
  ],
  "count": 49,
  "tables": 32,
  "target_memory_mib": 1024,
  "target_cpus": 1
}
